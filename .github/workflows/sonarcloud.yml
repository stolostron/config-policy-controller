name: Sonarcloud scan

on:
  workflow_run:
    workflows:
      - KinD tests
    types:
      - completed

jobs:
  sonarcloud:
    uses: stolostron/governance-policy-framework/.github/workflows/sonarcloud.yml@415c30b70aaa22762674202dc7ad953ac92f539d # main
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
